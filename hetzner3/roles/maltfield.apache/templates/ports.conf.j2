# {{ ansible_managed }}

################################################################################
# File:    ports.conf
# Version: 0.1
# Purpose: List the ports that apache listens-on
# Author:  Michael Altfield <michael@michaelaltfield.net>
# Created: 2024-09-10
# Updated: 2024-09-10
################################################################################

# If you just change the port or add more ports here, you will likely also
# have to change the VirtualHost statement in
# /etc/apache2/sites-enabled/000-default.conf

# 8000 is used for all our varnish backends --maltfield
#Listen {{ ansible_default_ipv4.address }}:8000
Listen 127.0.0.1:8000

# 8010 is used as a catch-all for private sites' certbot renewals --maltfield
#Listen {{ ansible_default_ipv4.address }}:8010
Listen 127.0.0.1:8010

# Apache doesn't use https (which is terminated by nginx) --maltfield

#<IfModule ssl_module>
#	Listen 443
#</IfModule>
#
#<IfModule mod_gnutls.c>
#	Listen 443
#</IfModule>

# vim: syntax=apache ts=4 sw=4 sts=4 sr noet
